rem Setup target filename and target directory

rem TARGET = target filename, max. 8 characters (without extension)
rem GRPDIR = group destination directory on SD card, max. 8 characters

set TARGET=BIGFACT
set GRPDIR=DEMO
